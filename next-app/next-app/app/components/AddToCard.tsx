'use client';
import React from 'react'

const AddToCard = () => {
  return (
    <div>
        <button onClick={() => alert("Added to cart!")}>
          Add to Cart
        </button>
    </div>
  )
}

export default AddToCard